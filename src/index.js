import React from 'react';
import ReactDOM from 'react-dom/client';
import Corebiz from './Corebiz';
import './styles/styles.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Corebiz />
  </React.StrictMode>
);

